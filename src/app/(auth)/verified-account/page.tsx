import VerifiedAccountPage from "@/components/pages/VerifiedAccountPage"

export default function Page({ searchParams }: { searchParams: SearchParams }) {
  return <VerifiedAccountPage searchParams={searchParams} />
}
