import UserVerifyPage from "@/components/pages/UserVerifyPage"

export default function Page({ searchParams }: { searchParams: SearchParams }) {
  return <UserVerifyPage searchParams={searchParams} />
}