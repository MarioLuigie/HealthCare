import UserVerificationResultPage from "@/components/pages/UserVerificationResultPage"

export default function Page({ searchParams }: { searchParams: SearchParams }) {
  return <UserVerificationResultPage searchParams={searchParams} />
}
