import UserVerificationPendingPage from "@/components/pages/UserVerificationPendingPage"

export default function Page({ searchParams }: { searchParams: SearchParams }) {
  return <UserVerificationPendingPage searchParams={searchParams} />
}